{
	"name": "with-react-native-web",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"build": "turbo run build",
		"clean": "turbo run clean && rm -rf node_modules",
		"dev": "turbo run dev"
	},
	"dependencies": {},
	"devDependencies": {
		"turbo": "2.0.3"
	}
}
